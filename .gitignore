# Dependencies
node_modules/
target/

# Env & secrets
.env
.env.local
.env.*.local
!.env.example
contracts/deployments.env

# Builds
.next/
out/
dist/
*.wasm

# Stellar / Soroban
contracts/**/target/
*.pem
*.secret

# Editor / OS
.DS_Store
Thumbs.db
.idea/
.vscode/

# Logs
*.log
npm-debug.log*

# impeccable-ignore-start
# Ephemeral output, runtime state, and per-dev overrides.
.impeccable/config.local.json
.impeccable/hook.cache.json
.impeccable/hook.pending.json
.impeccable/*.png
.impeccable/live/
# Keep tracked: .impeccable/config.json, .impeccable/live/config.json,
# .impeccable/design.json, .impeccable/critique/*.md
# impeccable-ignore-end

# Design tooling (impeccable skill), not project code
.agents/
skills-lock.json
frontend/mockup/
